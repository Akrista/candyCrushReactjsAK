const ScoreBoard = ({ score }) => {
  return (
    <div className="bruh">
      <div className="score-board">
        <h3>score</h3>
        <h1>{score}</h1>
      </div>
    </div>
  );
};

export default ScoreBoard;
